{
  "name": "ai-car-runner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "wrangler": "^4.0.0"
  }
}
